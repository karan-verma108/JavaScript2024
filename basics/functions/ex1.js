const greet = (name) => {
  console.log(`${name}, welcome to the team`);
};

// greet('rocky');

/*regular function expression

const result = function sum(a,b) {
    return a + b;
}

console.log(result(4, 4)); */

//anonymous function expression

const result = function (a, b) {
  return a + b;
};

console.log(result(4, 4));
